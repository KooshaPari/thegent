[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "thegent-crypto"
version = "0.1.0"
description = "BKM-03: Sign/verify/hash for thegent"
requires-python = ">=3.12"

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "thegent_crypto"
