// Auto-generated usage examples for types
// Source: generate-api-docs.py

import { Complexity, Deliverable, Priority, SubagentType, Task, TaskMetadata, TaskOutput, TaskOutputStatus, TaskStep, TaskVisibility, validate_allowed_agents, validate_depends } from "./types";

// Create a Complexity instance
const complexity = new Complexity();

// Create a Deliverable instance
const deliverable = new Deliverable();

// Create a Priority instance
const priority = new Priority();

// Create a SubagentType instance
const subagenttype = new SubagentType();

// Create a Task instance
const task = new Task();
task.validate_allowed_agents(undefined as unknown as Array<string>, undefined as unknown as any);
task.validate_depends(undefined as unknown as Array<string>);

// Create a TaskMetadata instance
const taskmetadata = new TaskMetadata();

// Create a TaskOutput instance
const taskoutput = new TaskOutput();

// Create a TaskOutputStatus instance
const taskoutputstatus = new TaskOutputStatus();

// Create a TaskStep instance
const taskstep = new TaskStep();

// Create a TaskVisibility instance
const taskvisibility = new TaskVisibility();

// Call validate_allowed_agents
validate_allowed_agents(undefined as unknown as any, undefined as unknown as Array<string>, undefined as unknown as any);
// Call validate_depends
validate_depends(undefined as unknown as any, undefined as unknown as Array<string>);
