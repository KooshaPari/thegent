// Auto-generated usage examples for auth_bridge
// Source: generate-api-docs.py

import { AuthBridge, SSOConfig, bridge_saml_response } from "./auth_bridge";

// Create a AuthBridge instance
const authbridge = new AuthBridge(undefined as unknown as any);
authbridge.bridge_saml_response("example_saml_response");

// Create a SSOConfig instance
const ssoconfig = new SSOConfig();

// Call bridge_saml_response
bridge_saml_response(undefined as unknown as any, "example_saml_response");
