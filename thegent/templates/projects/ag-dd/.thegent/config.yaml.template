# thegent project config template
project:
  name: "{{PROJECT_NAME}}"
  tenant_id: "{{TENANT_ID}}"
  product_id: "{{PRODUCT_ID}}"

standards:
  template: "ag-dd"
  template_version: "1.0.0"
  fail_fast: true
