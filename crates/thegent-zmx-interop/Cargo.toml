[package]
name = "thegent-zmx-interop"
version = "0.1.0"
edition = "2021"
description = "Zig-Rust C ABI interop POC: Rust calls zmx (Zig session manager) via C ABI"
authors = ["thegent"]
license = "MIT"

[features]
default = []
zmx-native = []

[dependencies]
thiserror = "2.0"
libc = "0.2"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
