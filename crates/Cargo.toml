[workspace]
resolver = "2"
members = [
    "harness-native",
    "thegent-benchmark",
    "thegent-cache",
    "thegent-crypto",
    "thegent-metrics",
    "thegent-plugin-host",
]
exclude = [
    "thegent-tool-detect",
]

[workspace.dependencies]
serde = "1"
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"

[profile.dev]
opt-level = 0
