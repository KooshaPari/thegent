[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "thegent-policy"
version = "0.1.0"
description = "Policy engine and compliance evaluation for thegent governance"
requires-python = ">=3.12"
authors = [{name = "thegent contributors"}]
