[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "thegent-jsonl"
version = "0.1.0"
description = "Native JSONL parsing for thegent"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "thegent_jsonl"
