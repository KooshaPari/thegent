# Migrated: harness-native

`harness-native` is now owned by `sharecli` as the native harness substrate for CLI process orchestration.

This tree is retained only as migration evidence for historical `thegent` scope recovery. Do not add new implementation work here; update the canonical crate in `sharecli/crates/harness-native`.
