[project]
name = "thegent-module-governance"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "thegent-audit",
]

[tool.uv.sources]
thegent-audit = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/thegent_governance_module"]
