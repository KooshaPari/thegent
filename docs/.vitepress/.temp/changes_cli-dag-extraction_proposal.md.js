import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { a as _export_sfc } from "./vue.DCJT_Tnz.js";
import "./app.js";
import "@vueuse/core";
import "./mermaid.OkLrB7RK.js";
import "mermaid";
const __pageData = JSON.parse('{"title":"CLI DAG Extraction — Proposal","description":"","frontmatter":{"title":"CLI DAG Extraction — Proposal","date":"2026-02-21T00:00:00.000Z","status":"implemented","owner":"agent-f (B90-W2-F1)","tags":["wl-120","b90","monolith-split","cli"]},"headers":[],"relativePath":"changes/cli-dag-extraction/proposal.md","filePath":"changes/cli-dag-extraction/proposal.md","lastUpdated":1771678191000}');
const _sfc_main = { name: "changes/cli-dag-extraction/proposal.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="proposal-extract-dag-command-handlers-from-cli-py" tabindex="-1">Proposal: Extract DAG Command Handlers from cli.py <a class="header-anchor" href="#proposal-extract-dag-command-handlers-from-cli-py" aria-label="Permalink to &quot;Proposal: Extract DAG Command Handlers from cli.py&quot;">​</a></h1><h2 id="problem-statement" tabindex="-1">Problem Statement <a class="header-anchor" href="#problem-statement" aria-label="Permalink to &quot;Problem Statement&quot;">​</a></h2><p><code>src/thegent/cli/commands/cli.py</code> had grown to 6,927 lines (pre-wave-2), far exceeding the 500-line target for a single module. The DAG command group (<code>dag_validate_cmd</code>, <code>dag_add_cmd</code>, <code>dag_update_cmd</code>, <code>dag_list_cmd</code>, <code>dag_run_cmd</code>, <code>dag_ready_cmd</code>, <code>dag_sync_cmd</code>, <code>dag_recover_cmd</code>, and related helpers) represented a discrete, testable responsibility that could be cleanly extracted without breaking the public CLI surface.</p><h2 id="why-this-extraction" tabindex="-1">Why This Extraction <a class="header-anchor" href="#why-this-extraction" aria-label="Permalink to &quot;Why This Extraction&quot;">​</a></h2><ol><li><p>undefined<strong>Single-responsibility</strong>: DAG lifecycle (add/validate/run/sync/recover) is a coherent domain unit. Combining it with session, model, plan, and infra commands in a single 6k-line file violates SRP and makes grep/navigation impractical.</p></li><li><p>undefined<strong>LOC reduction</strong>: Extraction reduces the cli.py monolith by ~620 lines (the size of <code>cli_dag.py</code>), moving it from 6,927 to ~6,307 lines — a step toward the 500-line ceiling mandated by WL-120 / WL-124.</p></li><li><p>undefined<strong>Independent testability</strong>: <code>cli_dag.py</code> can be unit-tested in isolation with stubbed helpers from <code>cli.py</code>, without importing the full 6k-line monolith.</p></li><li><p>undefined<strong>Parallel extraction enabled</strong>: Once DAG handlers are isolated, other command groups (session, infra, plan, model, team, run) can be extracted in parallel by separate agents without merge conflicts.</p></li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Extract DAG CLI command handlers to <code>src/thegent/cli/commands/cli_dag.py</code>. Keep all helper functions and business logic in <code>cli.py</code> (or <code>impl.py</code>/<code>dag_impl.py</code>). The new module imports the helpers it needs from <code>cli.py</code> and delegates to them.</p><p><code>cli.py</code> does <strong>not</strong> re-export <code>cli_dag.py</code> — the DAG sub-app wires its own commands by importing <code>cli_dag.py</code> directly. This is forward-compatible with full monolith removal when all groups are extracted.</p><h2 id="acceptance-criteria" tabindex="-1">Acceptance Criteria <a class="header-anchor" href="#acceptance-criteria" aria-label="Permalink to &quot;Acceptance Criteria&quot;">​</a></h2><ul><li><code>cli_dag.py</code> exists and contains all <code>dag_*_cmd</code> handlers.</li><li><code>cli.py</code> still exports existing public symbols for backwards compatibility.</li><li><code>python -c &quot;from thegent.cli.commands.cli_dag import dag_validate_cmd&quot;</code> exits 0.</li><li>Existing DAG-related tests pass without modification.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("changes/cli-dag-extraction/proposal.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const proposal = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  proposal as default
};
