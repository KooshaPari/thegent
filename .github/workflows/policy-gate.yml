name: Policy Gate

on:
  pull_request:
    types: [opened, synchronize, reopened, edited, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: read

jobs:
  policy-gate:
    uses: KooshaPari/phenotypeActions/.github/workflows/policy-gate.yml@main
