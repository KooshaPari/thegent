name: CI (Minimal)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

# Tests run locally via pre-commit/pre-push hooks
# This is a placeholder for PR status checks

jobs:
  ci-placeholder:
    name: CI Placeholder (Local Hooks)
    runs-on: ubuntu-latest
    steps:
      - name: Pass
        run: echo "Tests run locally via pre-commit/pre-push hooks. See .pre-commit-config.yaml"
