name: Security (SAST)
on:
  push:
    branches: [main, develop]
  pull_request:
  schedule: [{cron: "0 2 * * *"}]
permissions:
  contents: read
  security-events: write

jobs:
  # Tier 1: Fast/Essential
  codeql:
    runs-on: ubuntu-latest
    continue-on-error: true  # Allow PRs to proceed even if CodeQL fails
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: github/codeql-action/init@v4  # Upgrade to v4
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/autobuild@v4
      - uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{matrix.language}}"
    strategy:
      matrix:
        language: [typescript, python, rust, go]

  # Tier 3: Expensive/Comprehensive (may have rate limits)
  semgrep:
    runs-on: ubuntu-latest
    continue-on-error: true  # Allow PRs to proceed even if Semgrep fails
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: returntocorp/semgrep-action@713efdd345f3035192eaa63f56867b88e63e4e5d
        with:
          generateSarif: true
          sarifFile: semgrep-results.sarif
        continue-on-error: true
      - uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: semgrep-results.sarif
          category: semgrep
