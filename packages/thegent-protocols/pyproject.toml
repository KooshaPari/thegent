[project]
name = "thegent-protocols"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "thegent-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"

[tool.hatch.build.targets.wheel]
packages = ["src/thegent_protocols"]
