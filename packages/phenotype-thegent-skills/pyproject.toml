[project]
name = "phenotype-thegent-skills"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "phenotype-thegent-core",
]

[tool.uv.sources]
phenotype-thegent-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/phenotype_thegent_skills"]
