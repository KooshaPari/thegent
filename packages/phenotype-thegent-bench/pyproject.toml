[project]
name = "phenotype-thegent-bench"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "phenotype-thegent-core",
    "phenotype-thegent-execution",
]

[tool.uv.sources]
phenotype-phenotype-thegent-core = { workspace = true }
phenotype-thegent-execution = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/thegent_bench"]
