---
audience: [developers, agents, pms]
---

# Docs

This docs tree is the canonical landing surface for `phenotype-config`.

## Start Here

- `Cargo.toml` for crate identity and dependencies
- `tests/` for crate behavior coverage

## Operating Model

Keep this crate focused on:

- Configuration loading
- Strongly typed validation
- Predictable defaults
- Small, composable testable units

Keep wave-specific work in `docs/sessions/<YYYYMMDD-descriptive-name>/`.
