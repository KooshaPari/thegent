# tasks

Title: Thegent Module Split Lane Bootstraps
Source: docs/changes/shared-modules/thegent-module-lane-bootstraps-v1/tasks.md

---

# Tasks: Thegent Module Split Lane Bootstraps

## Lane 1 — thegent-app

- [x] Define lane ownership and command ownership for app split execution.
- [x] Publish bootstrap command and expected worktree path.
- [x] Add bootstrap/verification Taskfile targets for app lane smoke.
- [x] Add smoke tests for `phench projects run` with `--module thegent-app` include/exclude filters.

## Lane 2 — thegent-mcp

- [x] Define lane ownership and command ownership for MCP split execution.
- [x] Publish bootstrap command and expected worktree path.
- [x] Add bootstrap/verification Taskfile targets for MCP lane smoke.
- [x] Add smoke tests for `phench projects run` with `--module thegent-mcp` include/exclude filters.

## Lane 3 — thegent-control-plane

- [x] Define lane ownership and command ownership for control-plane split execution.
- [x] Publish bootstrap command and expected worktree path.
- [x] Add bootstrap/verification Taskfile targets for control-plane lane smoke.
- [x] Add smoke tests for `phench projects run` with `--module thegent-control-plane` include/exclude filters.

## Lane 4 — thegent-execution

- [x] Define lane ownership and command ownership for execution split execution.
- [x] Publish bootstrap command and expected worktree path.
- [x] Add bootstrap/verification Taskfile targets for execution lane smoke.
- [x] Add smoke tests for `phench projects run` with `--module thegent-execution` include/exclude filters.

## Lane 5 — thegent-governance

- [x] Define lane ownership and command ownership for governance split execution.
- [x] Publish bootstrap command and expected worktree path.
- [x] Add bootstrap/verification Taskfile targets for governance lane smoke.
- [x] Add smoke tests for `phench projects run` with `--module thegent-governance` include/exclude filters.

## Completion Criteria

- All items are represented by test coverage and docs artifacts.
- Lane bootstrap commands can be executed without schema changes.
- `Taskfile` has per-lane smoke tasks and a lane index command.