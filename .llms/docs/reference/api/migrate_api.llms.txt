# migrate_api

Source: docs/reference/api/migrate_api.md

---

# migrate API Reference

> **Source**: `src/thegent/task/migrate.py`

Migration tool for converting legacy task formats to YAML frontmatter.

---

## migrate_legacy_task_to_yaml_frontmatter

```
migrate_legacy_task_to_yaml_frontmatter(content: str)
```

Convert legacy task format to YAML frontmatter + Markdown.

**Parameters**:

- `content`: Legacy task content

**Returns**: YAML frontmatter + Markdown content

---

## migrate_work_stream_entry_to_task

```
migrate_work_stream_entry_to_task(task_id: str, title: str, source: str, priority: str, depends: str, work_stream_path: Any)
```

Convert a WORK_STREAM.md table entry to a task dictionary.

**Parameters**:

- `task_id`: Task ID
- `title`: Task title
- `source`: Source document
- `priority`: Priority (P1, P2, P3)
- `depends`: Dependencies (comma-separated or "-")
- `work_stream_path`: Optional path to WORK_STREAM.md for extracting full details

**Returns**: Task dictionary ready for YAML frontmatter

---

## migrate_work_stream_to_tasks

```
migrate_work_stream_to_tasks(work_stream_path: Path, tasks_dir: Path, dry_run: bool)
```

Migrate WORK_STREAM.md entries to task files.

**Parameters**:

- `work_stream_path`: Path to WORK_STREAM.md
- `tasks_dir`: Directory to write task files
- `dry_run`: If True, don't write files, just return what would be created

**Returns**: dict with migration results

---
