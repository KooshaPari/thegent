# 13_NEXT_WAVE_G

Source: docs/sessions/20260324-phenotype-local-worktree-forest-blocker-matrix/13_NEXT_WAVE_G.md

---

# 13_NEXT_WAVE_G — next 24 items (6 × 4)

**Follows** `07`–`12`. **Snapshot:** 2026-03-24. **Inventory:** skip unless a dedicated inventory session. **Six slices** = optional parallel execution.

## Slice 1 — GitHub / repo policy (4)

1. **Branch protection** on default branch: required checks, no direct pushes if policy says so.
2. **CODEOWNERS** update for `apps/runtime/src/secrets/` and `pty/` if team split.
3. **Dependabot** or **Renovate** config review: pin major bumps, group minor.
4. **Workflow pins**: GitHub Actions use **SHA** or version tags, not floating `@main` for third-party actions.

## Slice 2 — Reproducibility & builds (4)

5. **Lockfile** policy: commit `bun.lock` (or project standard); no hand-edited locks.
6. **Deterministic** installs: document `bun install --frozen-lockfile` in CI.
7. **Node/Bun** version in `package.json` `engines` or `.tool-versions` / `mise`.
8. **Clean CI** runner: cache keys include lockfile hash.

## Slice 3 — Supply chain & SBOM (4)

9. **SBOM** export for release artifacts (if org requires): `syft` or equivalent.
10. **OSV** / **npm audit** gate: fail on critical unless waived with ticket.
11. **License allowlist** for new dependencies before merge.
12. **Vendoring** policy: avoid vendoring unless exception.

## Slice 4 — Feature safety & flags (4)

13. **Feature flags** (if product uses them): no secrets in flag payloads.
14. **Kill switch** doc for risky features (runtime kill via config).
15. **Gradual rollout** plan for PTY/secrets changes (canary users or internal only).
16. **Rollback** feature flags independent of code rollback where possible.

## Slice 5 — People & process (4)

17. **Onboarding** checklist: clone, worktree, `bun install`, `bun test` path.
18. **Reviewer load**: spread reviews; no single reviewer on all secrets PRs.
19. **Vacation** handoff: second owner for bus-critical modules.
20. **Meeting** cadence optional: async status in issue instead.

## Slice 6 — Halt / pivot (4)

21. **Stop adding waves** (H, I, …) until **07–13** execution progress is recorded.
22. **Merge** wave files into a single **ACTIVE_BACKLOG.md** if maintenance cost exceeds value.
23. **Archive** waves A–G to `docs/sessions/archive/20260324-worktree-waves/` when superseded.
24. **Pivot**: if priority shifts to product features, **freeze** worktree queue and reopen later.

---

**Roles:** **DevOps / admin** (1–4), **Build** (5–8), **AppSec / compliance** (9–12), **Product + SRE** (13–16), **Eng manager** (17–20), **Tech lead** (21–24).