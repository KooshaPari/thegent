version: 3
rules:
  - rule:
      paths:
        - "**"
      checks:
        - detector_id: HighEntropyString
          path: bundle/npm/.npmrc
        - detector_id: Base64Secret
        - detector_id: AWSKeyDetector
        - detector_id: GitHubTokenDetector
