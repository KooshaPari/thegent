#!/bin/bash
# migration script to overhaul shell scripts into Rust/Python

echo "Migrating shell scripts to native toolchain..."

# This script is now replaced by the native 'thegent-hooks' binary.
# Use 'thegent-hooks' directly or 'thegent' CLI for all development tasks.

/Users/kooshapari/temp-PRODVERCEL/485/kush/thegent/crates/target/release/thegent-hooks help
