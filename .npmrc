# Suppress npm audit for known-unfixable transitive vulnerabilities (no upstream fix available):
# - GHSA-67mh-4wv8-2f99: esbuild dev-server SSRF (vite depends on esbuild <=0.24.2)
# - GHSA-38c4-r59v-3vqw: markdown-it ReDoS (@orama/plugin-vitepress depends on markdown-it 13-14)
#   Both require upstream releases to fix (vite/esbuild and @orama/plugin-vitepress chains).
audit=false
