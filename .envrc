# direnv configuration for thegent
# https://direnv.net

# Load .env file
dotenv

# Add local bin to PATH
PATH_add bin
PATH_add .venv/bin

# Set PYTHONPATH to include src
export PYTHONPATH="$PWD/src:$PYTHONPATH"

# UI Hints
export THGENT_DX_MODE=strict
