# BytePort Orchestrator Dependencies

# Core dependencies
psutil>=7.2.2          # Process and system monitoring
pyyaml>=6.0.3            # YAML configuration
python-dotenv>=1.2.2   # Environment variable loading
watchdog>=6.0.0        # File watching for dev mode
httpx>=0.28.1          # HTTP client

# Pheno-SDK TUI-Kit dependencies
textual>=8.2.4        # TUI framework (from tui-kit)
rich>=15.0.0           # Rich terminal formatting
asyncio-mqtt>=0.16.2   # MQTT support (from tui-kit)
pydantic>=2.13.3        # Data validation

# Optional: Monitoring widgets
GPUtil>=1.4.0          # GPU monitoring (optional)

# Cloudflare API for DNS management (used by KInfra)
cloudflare>=4.3.1      # Cloudflare API SDK

# KInfra will be imported from ~/KInfra/libraries/python (no pip install needed)
# Pheno-SDK kits will be installed via editable installs (see setup.sh)
