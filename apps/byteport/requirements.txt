# BytePort Orchestrator Dependencies

# Core dependencies
psutil>=5.9.0          # Process and system monitoring
pyyaml>=6.0            # YAML configuration
python-dotenv>=1.0.0   # Environment variable loading
watchdog>=3.0.0        # File watching for dev mode
httpx>=0.28.0          # HTTP client

# Pheno-SDK TUI-Kit dependencies
textual>=0.45.0        # TUI framework (from tui-kit)
rich>=13.0.0           # Rich terminal formatting
asyncio-mqtt>=0.13.0   # MQTT support (from tui-kit)
pydantic>=2.0.0        # Data validation

# Optional: Monitoring widgets
GPUtil>=1.4.0          # GPU monitoring (optional)

# Cloudflare API for DNS management (used by KInfra)
cloudflare>=3.0.0      # Cloudflare API SDK

# KInfra will be imported from ~/KInfra/libraries/python (no pip install needed)
# Pheno-SDK kits will be installed via editable installs (see setup.sh)
