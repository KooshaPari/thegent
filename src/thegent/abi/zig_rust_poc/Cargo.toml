[package]
name = "zig-rust-poc"
version = "0.1.0"
edition = "2021"

[lib]
name = "zig_rust_poc"
crate-type = ["lib", "staticlib"]

[dependencies]
# No external dependencies needed for simple C ABI call
